<h1>Hi from Writeaway!</h1>

<p>We got a request to reset your password. To proceed, please visit the following link</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>

<div style="font-style: italic;">If you did not request a new password at Writeaway, just ignore this email.</div>
